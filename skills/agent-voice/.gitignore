# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv*/
venv/
*.egg-info/

# 模型权重与第三方引擎（体积过大，按 DEPLOYMENT.md 获取）
deployment/models/
deployment/MOSS-TTS-Nano/

# 运行时产物
*.log
*.lock
*.sock

# 模型权重（体积过大，按 DEPLOYMENT.md 获取）
scripts/MOSS-TTS-Nano/models/
scripts/MOSS-TTS-Nano/generated_audio/

# 部署期产物（selftest 报告 / T5 临时测量输出 / 实例化后的 plist 含本机路径）
scripts/selftest_report.json
scripts/_t1.wav
scripts/_t5_bs*.json
scripts/com.moss.ttsd.plist
scripts/tools/*.json

# CER 验证用的 ASR 模型（体积大，setup.sh/README 指引下载）
scripts/models/

# skill-creator 工作区（过程产物，不入库）
skills/*-workspace/

执行 ARS Stage 2: WRITE。

研究目标：
{{research_goal}}

研究阶段 artifacts：
{{research_artifact_paths}}

已有论文路径：`{{paper_path}}`

阶段计划：
{{stage_plan}}

请按计划调用或遵循 `academic-paper` 的合适模式（如 `plan`、`full`、`outline-only`、`lit-review`）。本阶段必须保留配置确认和 outline approval，不要把未确认的大纲直接推进到 integrity。

完成条件：
- 产出论文草稿或等价 draft artifact。
- 记录 Paper Configuration、Outline、Citation Audit 等关键材料。
- 用户确认 Stage 2 checkpoint。
如果缺少写作输入、用户未确认配置/大纲，或草稿尚不可交给 integrity gate，返回 `blocked`。

## Goal

重新检查 `{{runtime_root_path}}` 是否存在，并确认之前的观察结果是否准确。

## Required Work

1. 重新检查 `{{runtime_root_path}}` 是否存在。
2. 如果存在，重新列出一级目录。
3. 如果新的检查结果和之前不一致，说明差异点。

## Deliverable Notes

只返回本次复检需要的结构化字段，不要自行决定后续分支。

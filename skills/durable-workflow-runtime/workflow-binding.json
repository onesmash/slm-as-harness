{
  "default_workflow_id": "demo-prompt-loop",
  "workflows": [
    {
      "workflow_id": "demo-prompt-loop",
      "flow_description": "demo",
      "start_input_schema": {
        "task_input": {
          "goal": "string"
        },
        "context": {
          "repo_root": "string"
        },
        "constraints": {
          "max_steps": "integer?"
        }
      }
    },
    {
      "workflow_id": "academic-research-pipeline",
      "flow_description": "academic research pipeline",
      "start_input_schema": {
        "task_input": {
          "research_goal": "string",
          "workflow_scope": "string?",
          "entry_stage": "string?",
          "preferred_modes": "object?",
          "paper_type": "string?",
          "target_venue": "string?",
          "citation_format": "string?"
        },
        "context": {
          "repo_root": "string",
          "source_materials_path": "string?",
          "paper_path": "string?",
          "material_passport_path": "string?",
          "output_dir": "string?"
        },
        "constraints": {
          "max_steps": "integer?",
          "require_user_checkpoints": "boolean?",
          "enable_claim_audit": "boolean?",
          "allow_format_render": "boolean?",
          "max_revision_loops": "integer?"
        }
      }
    },
    {
      "workflow_id": "ios-ai-assisted-development-flow",
      "flow_description": "Run the AI-assisted development path from brainstorming-nex through planning, implementation, release QA, and pre-merge verification, with knowledge-base available as supporting context throughout.",
      "start_input_schema": {
        "task_input": {
          "goal": "string",
          "preferred_change_name": "string?"
        },
        "context": {
          "repo_root": "string",
          "source_doc_url": "string?",
          "source_skill_url": "string?",
          "agent_device_mode": "string?",
          "agent_device_app_id": "string?",
          "agent_device_artifact_path": "string?",
          "agent_device_device": "string?",
          "agent_device_session": "string?",
          "agent_device_replay_suite": "string?",
          "agent_device_evidence_dir": "string?",
          "agent_device_expected_version": "string?"
        },
        "constraints": {
          "max_steps": "integer?"
        }
      }
    },
    {
      "workflow_id": "performance-optimization-cycle",
      "flow_description": "适用于性能敏感内核的通用、可恢复优化闭环：从头脑风暴、研究、直接实现与评审到知识库更新，并将已验证结果带入下一轮优化。",
      "start_input_schema": {
        "task_input": {
          "goal": "string",
          "baseline_cycles": "integer?"
        },
        "context": {
          "repo_root": "string"
        },
        "constraints": {
          "max_steps": "integer?",
          "max_cycles": "integer?"
        }
      }
    },
    {
      "workflow_id": "co-storm-autonomous-research",
      "flow_description": "Autonomous Co-STORM-style research: warm-start stable expert perspectives, collect one grounded result from an independent subagent for each expert in every round, merge any newly retrieved evidence into the shared registry, let the Moderator adjudicate semantic coverage with deterministic guardrails, reorganize the knowledge map, and synthesize a cited complete or explicitly partial report whose body uses compact numeric [n] markers and whose final Evidence index maps each used marker to its exact registry source locator, without user intervention; after bounded self-repair, hard blocks terminate with a partial handoff. NOTE: the warm-start stage seeds the full planned topic set with no numeric ceiling and every seeded topic must be assessed before the report can be complete; set constraints.max_rounds (default 8) and constraints.max_steps (default 24) large enough to cover the planned topics, otherwise the run ends as an explicitly partial report. constraints.coverage_threshold remains only a deterministic lower bound on distinct assessed topics.",
      "start_input_schema": {
        "task_input": {
          "goal": "string",
          "deliverable_type": "string?",
          "research_scope": "string?",
          "source_policy": "string?"
        },
        "context": {
          "repo_root": "string",
          "source_materials_path": "string?",
          "output_dir": "string?"
        },
        "constraints": {
          "max_steps": "integer?",
          "max_rounds": "integer?",
          "min_evidence_items": "integer?",
          "coverage_threshold": "integer?",
          "max_reorganizations": "integer?"
        }
      }
    },
    {
      "workflow_id": "earnings-reviewer",
      "flow_description": "Process a covered name's earnings event: pull the print, transcript, and filings; analyze the call; update and audit the coverage model; then stage a post-earnings note draft for senior-analyst review without publishing.",
      "start_input_schema": {
        "task_input": {
          "ticker": "string?",
          "reporting_period": "string?",
          "goal": "string?",
          "skip_note": "boolean?"
        },
        "context": {
          "repo_root": "string",
          "coverage_model_path": "string?",
          "transcript_path": "string?",
          "filings_path": "string?"
        },
        "constraints": {
          "max_steps": "integer?"
        }
      }
    }
  ]
}

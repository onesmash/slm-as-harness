---
name: academic-research-pipeline
description: |
  Slash-only shortcut for durable workflow `academic-research-pipeline`. Load only when the
  user explicitly invokes `/academic-research-pipeline`, then continue through
  `/durable-workflow-runtime academic-research-pipeline` followed by the user's raw trailing
  text.
---

/durable-workflow-runtime academic-research-pipeline followed by the user's raw trailing text after `/academic-research-pipeline`

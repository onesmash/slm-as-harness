---
name: workflow:academic_research_pipeline
description: |
  Slash-only shortcut for durable workflow `academic_research_pipeline`. Load only when the
  user explicitly invokes `/workflow:academic_research_pipeline`, then continue through
  `/durable-workflow-runtime academic_research_pipeline` followed by the user's raw trailing
  text.
---

/durable-workflow-runtime academic_research_pipeline followed by the user's raw trailing text after `/workflow:academic_research_pipeline`

---
name: academic_research_pipeline
description: |
  Slash-only shortcut for durable workflow `academic_research_pipeline`. Load only when the
  user explicitly invokes `/academic_research_pipeline`, then continue through
  `/durable-workflow-runtime academic_research_pipeline` followed by the user's raw trailing
  text.
---

/durable-workflow-runtime academic_research_pipeline followed by the user's raw trailing text after `/academic_research_pipeline`

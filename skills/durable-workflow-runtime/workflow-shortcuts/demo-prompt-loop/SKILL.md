---
name: demo-prompt-loop
description: |
  Slash-only shortcut for durable workflow `demo-prompt-loop`. Load only when the
  user explicitly invokes `/demo-prompt-loop`, then continue through
  `/durable-workflow-runtime demo-prompt-loop` followed by the user's raw trailing
  text.
---

/durable-workflow-runtime demo-prompt-loop followed by the user's raw trailing text after `/demo-prompt-loop`
